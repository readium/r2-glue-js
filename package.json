{
  "name": "r2-glue-js",
  "private": true,
  "devDependencies": {
    "@types/jest": "^23.3.9",
    "jest": "^23.6.0",
    "lerna": "^3.4.3",
    "prettier": "^1.15.2",
    "rimraf": "^2.6.2",
    "ts-jest": "^23.10.4",
    "tslint": "^5.11.0",
    "tslint-config-airbnb": "^5.11.1",
    "tslint-eslint-rules": "^5.4.0",
    "typescript": "^3.1.6"
  },
  "scripts": {
    "test": "jest --verbose",
    "lint": "tslint --project tsconfig.json --config tslint.json -t stylish 'packages/**/*.ts'",
    "build": "tsc -b packages",
    "publish": "npm run build && lerna publish"
  }
}
