// Ensure that the statements here are exporting only the *named* receiver class for each module
//
// Example:
// export { FooBarReceiver } from '../modules/foo-bar/receiver';

export { EventHandlingReceiver } from './modules/event-handling/receiver';
